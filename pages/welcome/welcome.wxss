/* pages/welcome.wxss */

.main {
  display: flex;
  flex-direction: column;
  /* align-items 属性定义flex子项在flex容器的当前行的侧轴（纵轴）方向上的对齐方式。 */
  align-items: center;
  /* justify-content 用于设置或检索弹性盒子元素在主轴方向上的对齐方式。 */
  justify-content: space-around;
  /* 
  为view设置高度的原因：
  默认情况下view会与其中包裹的元素的高度一致
  没办法让元素在其中散开排列
   */
  height: 1500rpx;
}

image {
  width: 150rpx;
  border-radius: 50%;
}

text {
  font-size: 64rpx;
}